package entities;

/**
 * ...
 * @author 
 */
class Entity
{
	
	//TODO: flesh this out, make other entities extend this
	/*
	 * for when I do doors, buttons, coins, etc.
	 * make a "currentState" and "savedState" here
	 * when resetting, currentState = savedState
	 * when hitting a checkpoint, savedState = currentState
	 * override state in the entites to use stuff like STATE_OPEN etc.
	 * can also save position etc. here for moving entities
	 * 
	 * */
	
	public function new() 
	{
		
	}
	
}